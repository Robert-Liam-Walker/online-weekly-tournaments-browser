diff --git a/pc/platform/gw_runtime.c b/pc/platform/gw_runtime.c
index d0e098c..09ee242 100644
--- a/pc/platform/gw_runtime.c
+++ b/pc/platform/gw_runtime.c
@@ -17,10 +17,18 @@
  * gwtool stores scalars in global initializers pre-swapped, but a slot holding the address of
  * another symbol is filled in by the linker and so cannot be swapped ahead of time. Each object
  * file lists those slots in section .gwfix$m; the markers below bracket the merged list. */
+#if defined(TARGET_WASM)
+/* wasm-ld provides __start_/__stop_ for the "gwfix" section gwtool emits on wasm32. */
+extern void *const __start_gwfix[];
+extern void *const __stop_gwfix[];
+#define gw_fixups_start (__start_gwfix - 1)
+#define gw_fixups_end (__stop_gwfix)
+#else
 #pragma section(".gwfix$a", read)
 #pragma section(".gwfix$z", read)
 __declspec(allocate(".gwfix$a")) static void *const gw_fixups_start[1] = {0};
 __declspec(allocate(".gwfix$z")) static void *const gw_fixups_end[1] = {0};
+#endif
 
 void gw_apply_fixups(void) {
   uint32_t **p = (uint32_t **)(gw_fixups_start + 1);
diff --git a/pc/tools/gwtool/gwtool.cpp b/pc/tools/gwtool/gwtool.cpp
index 37c8401..65f2e15 100644
--- a/pc/tools/gwtool/gwtool.cpp
+++ b/pc/tools/gwtool/gwtool.cpp
@@ -78,6 +78,8 @@ cl::opt<std::string> SymPrefix("prefix", cl::desc("External symbol prefix"), cl:
 cl::opt<std::string> ImportsFile("imports",
                                  cl::desc("Write undefined external symbols to this file"),
                                  cl::init(""));
+cl::opt<std::string> TargetTriple("triple", cl::desc("Output target triple"),
+                                  cl::init("i686-pc-windows-msvc"));
 cl::opt<std::string> CPUName("mcpu", cl::desc("x86 CPU"), cl::init("pentium4"));
 cl::opt<std::string> FeatureStr("mattr", cl::desc("x86 features"),
                                 cl::init("+sse,+sse2,+cmov,+cx8,+mmx,+fxsr"));
@@ -87,6 +89,10 @@ cl::opt<bool> NoSafeArith("no-safe-arith",
 cl::opt<bool> NoSwap("no-swap", cl::desc("Debug: do not byte-swap memory accesses"),
                      cl::init(false));
 
+// wasm32 has no grouped COFF sections: the fixup table becomes a plain "gwfix" section that
+// wasm-ld brackets with __start_gwfix/__stop_gwfix (see gw_runtime.c under TARGET_WASM).
+bool IsWasmTarget = false;
+
 [[noreturn]] void fatal(const Twine &Msg) {
   errs() << "gwtool: error: " << InputFilename << ": " << Msg << "\n";
   exit(1);
@@ -487,7 +493,7 @@ private:
     auto *AT = ArrayType::get(P, Entries.size());
     auto *Table = new GlobalVariable(M, AT, /*isConstant=*/true, GlobalValue::InternalLinkage,
                                      ConstantArray::get(AT, Entries), "__gw_fixups");
-    Table->setSection(".gwfix$m");
+    Table->setSection(IsWasmTarget ? "gwfix" : ".gwfix$m");
     Table->setAlignment(Align(4));
     appendToUsed(M, {Table});
   }
@@ -640,6 +646,11 @@ int main(int argc, char **argv) {
   LLVMInitializeX86TargetMC();
   LLVMInitializeX86AsmPrinter();
   LLVMInitializeX86AsmParser();
+  LLVMInitializeWebAssemblyTargetInfo();
+  LLVMInitializeWebAssemblyTarget();
+  LLVMInitializeWebAssemblyTargetMC();
+  LLVMInitializeWebAssemblyAsmPrinter();
+  LLVMInitializeWebAssemblyAsmParser();
   cl::ParseCommandLineOptions(argc, argv, "gwtool: Melee PC port big-endian-memory transformer\n");
 
   LLVMContext Ctx;
@@ -657,7 +668,13 @@ int main(int argc, char **argv) {
   if (!OldDL.isBigEndian())
     fatal("expected a big-endian data layout");
 
-  Triple NewT("i686-pc-windows-msvc");
+  Triple NewT(TargetTriple);
+  IsWasmTarget = NewT.isWasm();
+  if (IsWasmTarget) {
+    // The x86 defaults do not apply; emscripten picks the wasm feature set at link time.
+    CPUName = "generic";
+    FeatureStr = "";
+  }
   std::string Error;
   const Target *Tgt = TargetRegistry::lookupTarget(NewT, Error);
   if (!Tgt)
